<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://code.jquery.com/jquery-3.7.1.js"
        integrity="sha256-eKhayi8LEQwp4NKxN+CfCh+3qOVUtJn3QNZ0TciWLP4=" crossorigin="anonymous"></script>
</head>

<body>
    <h1>이미지 바꾸기</h1>
    <img src="/abc/images/q.jpg" width="300" height="300">
    <br />
    <% for(let i=0; i < fruit.length; i++) { %>
        <button class="btn <%= fruit[i].name %>">
            <%= fruit[i].kor %>
            
        </button>
        <% } %>

            <script>
                $('.btn').click(function() {
                    let name1; //파일명
                    if ($(this).hasClass('apple')) {
                        name1 = 'apple';
                    } else if ($(this).hasClass('banana')) {
                        name1 = 'bananas';
                    } else if ($(this).hasClass('grapes')) {
                        name1 = 'grapes';
                    } else if ($(this).hasClass('peache')) {
                        name1 = 'peaches';
                    }

                    $('img').attr("src", `/abc/images/${name1}.jpg`);

                });

            </script>
</body>

</html>